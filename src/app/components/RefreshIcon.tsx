import { IoMdRefresh } from "@react-icons/all-files/io/IoMdRefresh";

export default function RefreshIcon({ color = "black" }: { color?: string }) {
  return <IoMdRefresh size={20} color={color} />;
}
